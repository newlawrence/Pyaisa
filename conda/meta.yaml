package:
  name: pysapp
  version: "0.8.4"

build:
  number: 4
  script_env:
    - BINSTAR_BUILD # [not win]
    - TRAVIS_BUILD_ID # [not win]
    - APPVEYOR_BUILD_ID # [not win]

source:
  path: ..

requirements:
  build:
    - python
    - numpy
    - swig # [not win]
    - pytest
  run:
    - python
    - numpy
    - pytest

about:
  license: MIT License
  home: https://github.com/newlawrence/Pysapp
  summary: "ISA Model computed in C++"
