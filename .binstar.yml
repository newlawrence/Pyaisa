package: pyaisa
platform: 
  - linux-64
engine:
  - python=3.5
env:
  - NUMPY=1.9
  - NUMPY=1.10
install:
  - conda update conda-build
build_targets:
  - conda
script:
  - CC=gcc CXX=g++ conda build recipe --numpy $NUMPY --quiet
